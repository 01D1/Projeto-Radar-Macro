---
context: phase
phase: 01-foundation-and-cleanup
plan: gaps
task: null
total_tasks: null
status: gaps_found
last_updated: 2026-05-10T19:09:15-03:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: MODULE-LEVEL-SYSEXIT — `src/main.py` lines 26-33 call `sys.exit(1)` at module scope. Any `import src.main` (including pytest) will kill the entire Python process when `ENV=production`. The gap closure plan MUST move this guard inside `app()`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Module-level sys.exit | Startup guard in src/main.py runs at import time, not inside app(). When ENV=production without keys, pytest dies entirely before collecting tests. | blocking | Gap closure plan must use Edit tool to move the guard block inside the `app()` function, then verify with `pytest tests/test_settings.py -x -q` passes without ENV=production killing the runner. |
| Worktree on OneDrive | git worktree add fails intermittently on OneDrive cloud-synced paths due to lock file timing. | advisory | Always use sequential execution (no `isolation="worktree"`) for this project. Config has no `use_worktrees` key — default would be true but sequential is safer here. |

<current_state>
Phase 1 execution complete for plans 01-01 and 01-02. Verifier ran and found gaps_found (2/4 must-haves). Gap closure needed before phase can be marked complete. The gsd-planner was interrupted before creating plan 01-03 (retry & logging infrastructure).

Test results: 10 passed, 5 failed (all failures are RED expected — retry/logger stubs not yet implemented).
</current_state>

<completed_work>

- Plan 01-01: Codebase consolidation + test bootstrap (commit 4e22978)
  - Windows venv recreated at OBSIDIAN root (.venv/, Python 3.10.11)
  - 5 test stub files created (tests/__init__.py, test_consolidation.py, test_retry.py, test_logger.py, test_settings.py, test_news_hunter_config.py)
  - sys.path manipulation removed from src/processing/pipeline.py
  - DEPRECATED.md added to pipeline banco completo/
  - Task Scheduler updated (ValuationBancario_Manha + Tarde → vault path)

- Plan 01-02: Credentials & config migration (commit c110096)
  - env renamed to .env, gitignore updated (env, .env, *.env)
  - Hardcoded Telegram token removed from news_hunter/config.py lines 60-61
  - REQUIRED_IN_PRODUCTION + model_post_init validation added to config/settings.py
  - Startup guard added to src/main.py (BUG: at module level — see blocking constraint)
  - Startup guard added to news_hunter/main.py
  - pipeline banco completo/config/settings.py wired to shared .env

- Code review: 01-REVIEW.md created (4 critical, 5 warning, 3 info)
- Verification: 01-VERIFICATION.md created — status: gaps_found, 2/4 must-haves
</completed_work>

<remaining_work>

Gap closure required (run `/gsd-plan-phase 1 --gaps`):

1. **FOUND-03 — Retry infrastructure:**
   - Create `src/utils/errors.py` with `IngestionError(Exception)` having `.cause` attribute
   - Extend `src/utils/retry.py`: add `jitter` parameter, raise `IngestionError` on exhaustion
   - All 4 tests in test_retry.py must turn GREEN

2. **FOUND-04 — Structured logging:**
   - Add `bind_run_id(prefix)` context manager to `src/utils/logger.py`
   - Returns `run_id = f"{prefix}-{uuid4().hex[:8]}"`
   - Injects `run_id` into all loguru records within context, clears on exit
   - `test_run_id_binding` must turn GREEN

3. **CR-04 fix — Module-level sys.exit:**
   - Move startup guard (lines 26-33) from module scope into `app()` in src/main.py
   - After fix: `pytest tests/ -x -q` must not die when ENV=production

4. Verify all tests green: `pytest tests/ -v` from 12_PYTHON/ with venv activated
</remaining_work>

<decisions_made>

- Venv at OBSIDIAN root (.venv/) — Windows Python 3.10.11 (stale macOS 3.9 deleted)
- pyproject.toml: requires-python lowered to >=3.10, hatchling build config added, pyyaml explicit
- Sequential execution (no worktrees) — OneDrive makes git worktrees unreliable
- IsolatedSettings subclass pattern for test_settings.py (env_ignore_empty workaround)
- patch('dotenv.load_dotenv') in test_news_hunter_config.py helper (import-time load_dotenv workaround)
- Both Task Scheduler tasks updated: ValuationBancario_Manha + ValuationBancario_Tarde
</decisions_made>

<blockers>

- FOUND-03/04 implementation missing (plan 01-03 never created) — gap closure will fix
- CR-04: module-level sys.exit in src/main.py — gap closure will fix
- No human actions pending
</blockers>

## Required Reading (in order)

1. `.planning/phases/01-foundation-and-cleanup/01-VERIFICATION.md` — gaps detail, structured for --gaps consumption
2. `.planning/phases/01-foundation-and-cleanup/01-REVIEW.md` — CR-04 detail (startup guard bug)
3. `.planning/phases/01-foundation-and-cleanup/01-PATTERNS.md` — exact code patterns for retry/logger implementation
4. `.planning/phases/01-foundation-and-cleanup/01-VALIDATION.md` — test commands per task

## Infrastructure State

- `.venv/`: Windows Python 3.10.11 at OBSIDIAN root — activate with `.venv\Scripts\Activate.ps1`
- Working dir for pytest: `Analista de Investimentos/12_PYTHON/` (pythonpath=["."] in pyproject.toml)
- Git repo: OBSIDIAN root (master branch)
- `.env` file: `Analista de Investimentos/12_PYTHON/.env` (not committed, gitignored)
- Task Scheduler: 2 tasks registered pointing to vault path

<context>
Phase 1 ran 2 of 3 planned plans. The planner was interrupted before creating plan 01-03. Gap closure (`/gsd-plan-phase 1 --gaps`) will read the VERIFICATION.md and create plans for retry infrastructure (FOUND-03), logging (FOUND-04), and the CR-04 module-level sys.exit bug. After gap closure executes, all 15 tests should be green and the verifier should pass.
</context>

<next_action>
Start with: `/clear` then `/gsd-plan-phase 1 --gaps`
This reads 01-VERIFICATION.md and creates gap closure plans for FOUND-03, FOUND-04, and CR-04.
</next_action>
