---
phase: 05-delivery-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Analista de Investimentos/12_PYTHON/src/dashboard/__init__.py
  - Analista de Investimentos/12_PYTHON/src/dashboard/data.py
  - Analista de Investimentos/12_PYTHON/src/delivery/pdf_report.py
  - Analista de Investimentos/12_PYTHON/pyproject.toml
  - Analista de Investimentos/12_PYTHON/tests/test_dashboard_data.py
  - Analista de Investimentos/12_PYTHON/tests/test_delivery_telegram.py
  - Analista de Investimentos/12_PYTHON/tests/test_delivery_pdf.py
  - Analista de Investimentos/12_PYTHON/tests/test_scheduler_delivery.py
  - Analista de Investimentos/scanner_quant_profit_b3/_style.py
  - Analista de Investimentos/scanner_quant_profit_b3/app.py
autonomous: true
requirements: [DEL-01, DEL-02, DEL-03, DEL-04, DEL-05]

must_haves:
  truths:
    - "src/dashboard/ is a valid Python package (has __init__.py)"
    - "data.py exports 4 @st.cache_data functions with unique names covering all 4 query needs"
    - "fpdf2>=2.7.0 is listed in pyproject.toml and installed in the venv"
    - "ReportGenerator stub exists in pdf_report.py and generate_from_fixture() returns bytes starting with b'%PDF'"
    - "All 4 test stub files exist with correct test function names matching the validation architecture"
    - "app.py registers all 4 inteligencia_* pages in _PAGES and updates nav column ratio to [1.4]+[1]*9"
    - "_style.py exports DARK_CSS constant extracted from app.py lines 16-135"
  artifacts:
    - path: "Analista de Investimentos/12_PYTHON/src/dashboard/__init__.py"
      provides: "Python package init (empty)"
    - path: "Analista de Investimentos/12_PYTHON/src/dashboard/data.py"
      provides: "4 @st.cache_data query functions"
      exports: [get_watchlist_summary, get_asset_detail, get_macro_panel, get_opportunities]
    - path: "Analista de Investimentos/12_PYTHON/src/delivery/pdf_report.py"
      provides: "ReportGenerator class with generate_from_fixture() for testing"
    - path: "Analista de Investimentos/12_PYTHON/tests/test_dashboard_data.py"
      provides: "DEL-01, DEL-02 test stubs"
    - path: "Analista de Investimentos/12_PYTHON/tests/test_delivery_telegram.py"
      provides: "DEL-03, DEL-04 Telegram test stubs"
    - path: "Analista de Investimentos/12_PYTHON/tests/test_delivery_pdf.py"
      provides: "DEL-05 PDF test stubs"
    - path: "Analista de Investimentos/12_PYTHON/tests/test_scheduler_delivery.py"
      provides: "DEL-04 scheduler test stubs"
    - path: "Analista de Investimentos/scanner_quant_profit_b3/_style.py"
      provides: "DARK_CSS constant"
    - path: "Analista de Investimentos/scanner_quant_profit_b3/app.py"
      provides: "Updated _PAGES with 4 intelligence pages registered"
  key_links:
    - from: "src/dashboard/data.py"
      to: "src/ingestion/db.py"
      via: "get_connection(DB_PATH) with try/finally conn.close()"
      pattern: "get_connection\\(DB_PATH\\)"
    - from: "scanner_quant_profit_b3/app.py"
      to: "pages/inteligencia_*.py"
      via: "st.Page() entries in _PAGES list"
      pattern: "inteligencia_watchlist"
    - from: "pages/inteligencia_*.py"
      to: "_style.py"
      via: "from _style import DARK_CSS"
      pattern: "from _style import DARK_CSS"
---

<objective>
Wave 0 foundation for Phase 5. Creates the data access layer (src/dashboard/data.py), the PDF report stub (src/delivery/pdf_report.py), the shared CSS module (_style.py), registers the 4 new pages in app.py, installs fpdf2, and creates all 4 test stub files with the correct test function signatures.

Purpose: Every subsequent plan in Phase 5 imports from data.py and reads DARK_CSS from _style.py. This plan must complete before any page files are written. Without the test stubs, Wave 1 executors cannot run the test suite.

Output: 10 files created/modified; fpdf2 installed; pytest on the 4 new test files exits 0 (stubs pass as xfail or skip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-delivery-layer/05-CONTEXT.md
@.planning/phases/05-delivery-layer/05-RESEARCH.md
@.planning/phases/05-delivery-layer/05-PATTERNS.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from verified codebase analysis. -->

From src/ingestion/db.py (verified lines 209-219):
```python
# get_connection() returns a plain sqlite3.Connection — NOT a context manager.
# NEVER use `with get_connection() as conn:` — always use try/finally conn.close()
def get_connection(db_path: str | Path) -> sqlite3.Connection:
    conn = sqlite3.connect(db_path)
    conn.row_factory = sqlite3.Row
    return conn

DB_PATH: Path  # canonical DB path constant
```

From scanner_quant_profit_b3/app.py (verified lines 138-167):
```python
_PAGES = [
    st.Page("pages/radar_quant.py",      title="Radar Quant",      icon="🎯"),
    st.Page("pages/valuation_engine.py", title="Valuation Engine", icon="📊"),
    st.Page("pages/performance.py",      title="Performance",      icon="📈"),
    st.Page("pages/calendario.py",       title="Calendário",       icon="📅"),
    st.Page("pages/agendador.py",        title="Agendador",        icon="⏱"),
]
pages = st.navigation(_PAGES, position="hidden")
c_logo, *c_navs = st.columns([1.6] + [1] * len(_PAGES))
```

thesis_versions table schema (from Phase 4 D-12):
```sql
CREATE TABLE thesis_versions (
    id TEXT PRIMARY KEY,
    ticker TEXT NOT NULL,
    version_num INTEGER NOT NULL,
    generated_at TEXT NOT NULL,
    input_hash TEXT NOT NULL,
    positioning TEXT NOT NULL,
    confidence TEXT NOT NULL,
    fair_value_brl REAL NOT NULL,
    dcf_deviation_flag INTEGER DEFAULT 0,
    thesis_json TEXT NOT NULL,
    diff_summary TEXT,
    UNIQUE(ticker, version_num)
);
CREATE VIEW thesis_latest AS
    SELECT * FROM thesis_versions WHERE (ticker, version_num) IN (
        SELECT ticker, MAX(version_num) FROM thesis_versions GROUP BY ticker
    );
```

opportunity_signals table schema (from Phase 4 D-18):
```sql
CREATE TABLE opportunity_signals (
    id TEXT PRIMARY KEY,
    ticker TEXT NOT NULL,
    computed_date TEXT NOT NULL,
    signal_type TEXT NOT NULL,
    description TEXT NOT NULL,
    conviction_score INTEGER NOT NULL,
    ingested_at TEXT NOT NULL,
    UNIQUE(ticker, computed_date, signal_type)
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/dashboard package, data.py query layer, and _style.py CSS module</name>
  <files>
    Analista de Investimentos/12_PYTHON/src/dashboard/__init__.py,
    Analista de Investimentos/12_PYTHON/src/dashboard/data.py,
    Analista de Investimentos/scanner_quant_profit_b3/_style.py
  </files>

  <read_first>
    - Analista de Investimentos/12_PYTHON/src/ingestion/db.py — read lines 200-230 to confirm get_connection() signature and DB_PATH type; verify it is NOT a context manager
    - Analista de Investimentos/scanner_quant_profit_b3/app.py — read lines 1-140 to extract the full DARK_CSS block (lines 16-135) verbatim for _style.py
    - Analista de Investimentos/12_PYTHON/src/scheduler.py — read lines 60-120 to confirm the try/finally conn.close() pattern used in job functions (Pattern B from PATTERNS.md)
    - PRE-CHECK (run before writing get_asset_detail): If ingestion.db has any thesis_versions rows, run `SELECT thesis_json FROM thesis_versions LIMIT 1` and inspect the JSON to confirm that `drivers` and `risks` are nested lists of `{title, description, impact}` objects. If the schema differs from this plan's documented structure, adapt the implementation accordingly.
    - Analista de Investimentos/12_PYTHON/src/intelligence_layer.py — read _compute_diff_summary() to confirm its return type (dict or str); this informs how 05-03 will extract prev_positioning in run_ticker().
  </read_first>

  <behavior>
    - src/dashboard/__init__.py: empty file (Python package marker)
    - data.py get_watchlist_summary(): returns list[dict] ordered by upside_pct DESC; each dict has keys: ticker, positioning, confidence, fair_value_brl, generated_at, price, pe_ratio, ev_ebitda, upside_pct
    - data.py get_asset_detail(ticker): returns dict with keys thesis (deserialized dict from json.loads(thesis_json)), dcf (dict), ltm (dict), multiples (dict), news (list of 5 dicts), macro (dict from get_macro_panel()); returns None if ticker not in thesis_latest
    - data.py get_macro_panel(): returns dict[str, list[dict]] with keys: selic, ipca_12m, ptax, cds_brasil, pib_nominal; each value is list of {date, value} dicts ordered chronologically (reversed from DESC query)
    - data.py get_opportunities(): returns list[dict] ordered by conviction_score DESC, LIMIT 10, from today's computed_date
    - All 4 functions decorated with @st.cache_data(ttl=300) — unique names, all in data.py only
    - All SQL uses ? placeholder — no f-string with ticker (T-05-01 rule)
    - get_connection() called with try/finally conn.close() — never with/as
    - _style.py: module docstring "Shared dark theme CSS for scanner_quant_profit_b3 pages."; DARK_CSS constant = verbatim CSS extracted from app.py; must be a valid Python string
    - Module-level logger: `log = get_logger(__name__)` in data.py
  </behavior>

  <action>
1. Create `Analista de Investimentos/12_PYTHON/src/dashboard/__init__.py` — empty file (no content).

2. Create `Analista de Investimentos/12_PYTHON/src/dashboard/data.py`:
   - Module docstring: "data.py\n-------\nDashboard query layer for Phase 5 Delivery.\n\nFluxo: ingestion.db → cached query functions → Streamlit pages\nUso:\n    from src.dashboard.data import get_watchlist_summary, get_asset_detail"
   - Imports: `from __future__ import annotations`, `import json`, `import streamlit as st`, `from src.ingestion.db import get_connection, DB_PATH`, `from src.utils.logger import get_logger`
   - `log = get_logger(__name__)`
   - Implement all 4 @st.cache_data(ttl=300) functions with the exact SQL verified in RESEARCH.md Code Examples:
     - `get_watchlist_summary()`: JOIN thesis_latest + financial_multiples + financial_dcf with correlated MAX(computed_date) subqueries; ORDER BY fd.upside_pct DESC; return [dict(r) for r in rows]
     - `get_asset_detail(ticker: str) -> dict | None`: ticker param = `?` placeholder; fetch thesis_row from thesis_latest WHERE ticker=?; if None return None; result = dict(thesis_row); result["thesis"] = json.loads(result["thesis_json"]); fetch dcf, ltm, multiples with ORDER BY computed_date DESC LIMIT 1; fetch news (5 rows) from news_articles WHERE ticker_tags LIKE ? with param `f'%"{ticker}"%'`; add `result["macro"] = get_macro_panel()` — macro is already @st.cache_data so this reuses the shared cache with no extra DB connection; return result; all fetchone() results wrapped in `dict(row) if row else {}`
     - `get_macro_panel()`: SERIES dict {selic:11, ipca_12m:433, ptax:1, cds_brasil:29039, pib_nominal:4380}; single connection for all 5 queries; ORDER BY date DESC LIMIT 365; result[name] = [dict(r) for r in reversed(rows)]
     - `get_opportunities()`: WHERE computed_date = (SELECT MAX(computed_date) FROM opportunity_signals); ORDER BY conviction_score DESC LIMIT 10

3. Create `Analista de Investimentos/scanner_quant_profit_b3/_style.py`:
   - Module docstring: `"""Shared dark theme CSS for scanner_quant_profit_b3 pages."""`
   - DARK_CSS constant: read the CSS block from app.py lines 16-135 and assign it verbatim as `DARK_CSS: str = """..."""`
   - The CSS must include all selectors: stDecoration, collapsedControl, stPageLink, div[data-baseweb], expanders (details/summary), stMetric, scrollbar
  </action>

  <verify>
    <automated>cd "Analista de Investimentos/12_PYTHON" && python -c "from src.dashboard.data import get_watchlist_summary, get_asset_detail, get_macro_panel, get_opportunities; print('imports OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `Analista de Investimentos/12_PYTHON/src/dashboard/__init__.py` exists (file exists, may be empty)
    - `data.py` contains `def get_watchlist_summary(` — grep verifiable
    - `data.py` contains `def get_asset_detail(` — grep verifiable
    - `data.py` contains `def get_macro_panel(` — grep verifiable
    - `data.py` contains `def get_opportunities(` — grep verifiable
    - `data.py` contains `@st.cache_data(ttl=300)` — appears 4 times
    - `data.py` contains `conn.close()` in try/finally blocks — no `with get_connection()` pattern
    - `data.py` contains `json.loads(result["thesis_json"])` — Pitfall 5 mitigation
    - `data.py` contains parameterized SQL only — no `f"...{ticker}..."` pattern
    - `_style.py` contains `DARK_CSS` and `stPageLink` (verifies CSS was extracted)
    - Python import succeeds: `from src.dashboard.data import get_watchlist_summary` exits 0
    - Python import succeeds: `from scanner_quant_profit_b3._style import DARK_CSS` — or `from _style import DARK_CSS` when run from scanner_quant_profit_b3/
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fpdf2 install + ReportGenerator stub + app.py page registration + all 4 test stubs</name>
  <files>
    Analista de Investimentos/12_PYTHON/pyproject.toml,
    Analista de Investimentos/12_PYTHON/src/delivery/pdf_report.py,
    Analista de Investimentos/12_PYTHON/tests/test_dashboard_data.py,
    Analista de Investimentos/12_PYTHON/tests/test_delivery_telegram.py,
    Analista de Investimentos/12_PYTHON/tests/test_delivery_pdf.py,
    Analista de Investimentos/12_PYTHON/tests/test_scheduler_delivery.py,
    Analista de Investimentos/scanner_quant_profit_b3/app.py
  </files>

  <read_first>
    - Analista de Investimentos/12_PYTHON/pyproject.toml — read lines 1-60 to find [project.dependencies] list; identify the insertion point (after pyyaml entry)
    - Analista de Investimentos/12_PYTHON/src/delivery/telegram_bot.py — read lines 1-50 to confirm TelegramBot class name, module imports, and the _MAX_MESSAGE_LENGTH constant; read lines 120-150 for existing method signatures (send_risk_alert pattern)
    - Analista de Investimentos/12_PYTHON/tests/test_scheduler_intelligence.py — read lines 1-60 for the exact module header, import pattern, monkeypatch style, and MagicMock usage to replicate in new test files
    - Analista de Investimentos/scanner_quant_profit_b3/app.py — read lines 135-175 to see exact _PAGES list and the st.columns nav line to be modified
  </read_first>

  <behavior>
    - pyproject.toml: "fpdf2>=2.7.0" added to [project.dependencies] after the pyyaml entry
    - pdf_report.py: ReportGenerator(FPDF) subclass with:
      - header() and footer() overrides
      - 8 section methods: _header_section, _thesis_section, _bull_bear_section, _drivers_risks_section, _valuation_section, _financials_section, _macro_section, _disclaimer
      - public generate(ticker: str, data: dict) -> bytes
      - generate_from_fixture(ticker: str) -> bytes using hardcoded minimal fixture data (for tests that don't need live DB)
      - CVM IN 598 disclaimer text exact wording from UI-SPEC.md
      - generate_from_fixture never writes to disk — uses bytes(self.output())
    - test_dashboard_data.py: mem_db fixture (in-memory SQLite with thesis_versions, thesis_latest VIEW, opportunity_signals, macro_series, financial_dcf, financial_multiples); tests: test_get_watchlist_summary, test_get_asset_detail, test_get_macro_panel, test_get_opportunities, test_cache_decorators_present
    - test_delivery_telegram.py: tests: test_send_thesis_alert_format, test_alert_only_on_change, test_alert_failure_does_not_raise, test_send_daily_brief_format (all using monkeypatch on TelegramBot.send)
    - test_delivery_pdf.py: tests: test_generate_returns_pdf_bytes, test_pdf_contains_required_strings, test_generate_under_30s (all using generate_from_fixture)
    - test_scheduler_delivery.py: tests: test_morning_brief_registered, test_morning_brief_cron_in_yaml, test_morning_brief_calls_send_daily_brief
    - app.py: append 4 st.Page entries to _PAGES before st.navigation() call; update column ratio line from `[1.6] + [1] * len(_PAGES)` to `[1.4] + [1] * len(_PAGES)` (len(_PAGES) will be 9 after addition)
  </behavior>

  <action>
1. Install fpdf2: run `pip install "fpdf2>=2.7.0"` in the 12_PYTHON venv. Then add `"fpdf2>=2.7.0",` to pyproject.toml [project.dependencies] after the pyyaml line.

2. Create `Analista de Investimentos/12_PYTHON/src/delivery/pdf_report.py`:
   - Module docstring matching intelligence_layer.py header style (Fluxo/Uso sections)
   - `from fpdf import FPDF`; `from src.utils.logger import get_logger`; `log = get_logger(__name__)`
   - `class ReportGenerator(FPDF):` subclass
   - `def header(self)`: set_font("Helvetica", style="B", size=14); cell title centered
   - `def footer(self)`: set_y(-15); set_font("Helvetica", style="I", size=8); page number
   - `def _header_section(self, ticker, generated_at)`: 14pt bold title "Relatório de Investimento — {ticker}"; 11pt date
   - `def _thesis_section(self, thesis)`: positioning (colored text via set_text_color RGB), confidence, rationale, summary_one_line; COMPRAR→RGB(22,163,74), MANTER→RGB(202,138,4), VENDER→RGB(220,38,38)
   - `def _bull_bear_section(self, thesis)`: two subsections — "Cenário Otimista" + bull_case prose; "Cenário Pessimista" + bear_case prose; 10pt body text
   - `def _drivers_risks_section(self, drivers, risks)`: use `with self.table(col_widths=(60, 110, 20), borders_layout="SINGLE_TOP_LINE") as table:` for each; header row: ("Driver", "Descrição", "Impacto") and ("Risco", "Descrição", "Severidade")
   - `def _valuation_section(self, dcf, multiples)`: fair_value_brl, upside_pct, pe_ratio, ev_ebitda, pbv_ratio (if present), dividend_yield (if present); formatted as 2-column text table
   - `def _financials_section(self, ltm)`: net_revenue, ebitda, net_income, fcf, net_debt all labeled "R$ {x:.1f}M" (divide by 1_000_000); None values shown as "—"
   - `def _macro_section(self, macro)`: 4 rows — Selic, IPCA 12m, PTAX, CDS Brasil; text table (no chart), latest value from each series list
   - `def _disclaimer(self)`: set_text_color(107, 114, 128); 8pt Italic; exact text from UI-SPEC.md: "Este relatório foi elaborado por analista de valores mobiliários autônomo, em conformidade com as disposições da Instrução CVM nº 598, de 3 de maio de 2018. As informações e análises contidas neste documento têm caráter exclusivamente informativo e não constituem oferta de compra ou venda de valores mobiliários, tampouco recomendação de investimento. O analista responsável certifica que as opiniões expressas neste relatório refletem de forma precisa, exclusiva e independente suas visões e análises pessoais. Investimentos em valores mobiliários envolvem riscos. Rentabilidade passada não é garantia de rentabilidade futura."
   - `def generate(self, ticker: str, data: dict) -> bytes`: calls all section methods with data keys; returns `bytes(self.output())`
   - `def generate_from_fixture(self, ticker: str) -> bytes`: creates internal fixture dict with minimal valid data; calls generate(ticker, fixture); NOTE: instantiate fresh ReportGenerator() inside this method to avoid reuse-after-output() pitfall — use `return ReportGenerator()._build_from_fixture(ticker)` pattern or simply call all sections on `self` inside the method; return bytes(self.output())

3. Create all 4 test files using exact patterns from PATTERNS.md:
   - `tests/test_dashboard_data.py`: mem_db fixture with monkeypatch on DB_PATH; all 5 test functions; use `get_watchlist_summary.clear()` before each cached call to reset TTL cache
   - `tests/test_delivery_telegram.py`: 4 test functions; monkeypatch on `src.delivery.telegram_bot.TelegramBot.send`
   - `tests/test_delivery_pdf.py`: 3 test functions; all use `ReportGenerator().generate_from_fixture("PETR4")`; test_pdf_contains_required_strings decodes as latin-1 and checks for "PETR4" and "CVM"
   - `tests/test_scheduler_delivery.py`: 3 test functions; test_morning_brief_registered checks `_JOB_REGISTRY`; test_morning_brief_cron_in_yaml loads schedules.yaml and checks for "morning_brief" job name; test_morning_brief_calls_send_daily_brief uses MagicMock bot + in-memory SQLite

4. Modify `scanner_quant_profit_b3/app.py`:
   - Read the existing _PAGES list
   - Append after the last existing entry (agendador):
     ```python
     st.Page("pages/inteligencia_watchlist.py",      title="Watchlist",       icon="🔭"),
     st.Page("pages/inteligencia_ativo.py",           title="Ativo",           icon="🧠"),
     st.Page("pages/inteligencia_macro.py",           title="Macro",           icon="🌐"),
     st.Page("pages/inteligencia_oportunidades.py",   title="Oportunidades",   icon="🏆"),
     ```
   - Change `[1.6] + [1] * len(_PAGES)` to `[1.4] + [1] * len(_PAGES)` in the st.columns line (per UI-SPEC.md nav bar contract)
  </action>

  <verify>
    <automated>cd "Analista de Investimentos/12_PYTHON" && pip show fpdf2 | grep Version && python -c "from fpdf import FPDF; print('fpdf2 OK')" && pytest tests/test_delivery_pdf.py -x -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `pyproject.toml` contains `fpdf2>=2.7.0` — grep verifiable
    - `pip show fpdf2` returns a version line (not empty)
    - `src/delivery/pdf_report.py` contains `class ReportGenerator(FPDF):` — grep verifiable
    - `pdf_report.py` contains `def generate_from_fixture(` — grep verifiable
    - `pdf_report.py` contains `CVM` in the disclaimer text (full text present, never truncated)
    - `pdf_report.py` contains `bytes(self.output())` — grep verifiable (no old fpdf dest='S' API)
    - `test_delivery_pdf.py` exists and contains `test_generate_returns_pdf_bytes` — grep verifiable
    - `test_delivery_pdf.py` contains `b"%PDF"` — grep verifiable
    - `pytest tests/test_delivery_pdf.py -x -q` exits 0
    - `test_dashboard_data.py` exists and contains `test_cache_decorators_present` — grep verifiable
    - `test_delivery_telegram.py` exists and contains `test_send_thesis_alert_format` — grep verifiable
    - `test_scheduler_delivery.py` exists and contains `test_morning_brief_registered` — grep verifiable
    - `app.py` contains `inteligencia_watchlist.py` — grep verifiable
    - `app.py` contains `[1.4] + [1] * len(_PAGES)` — grep verifiable (nav ratio updated)
    - `pytest tests/test_dashboard_data.py tests/test_delivery_telegram.py tests/test_delivery_pdf.py tests/test_scheduler_delivery.py -x -q` exits 0 (stubs may xfail but must not error)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data.py → ingestion.db | Read-only DB access via ticker param from Streamlit selectbox |
| pdf_report.py → filesystem | PDF must never write to disk (D-17 — in-memory only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | data.py SQL queries | mitigate | All SQL uses `?` parameterized placeholders — no f-string with ticker anywhere in data.py |
| T-05-03 | Tampering | pdf_report.py | mitigate | generate() and generate_from_fixture() return `bytes(self.output())` — no file write, no path traversal possible (D-17) |
| T-05-04 | Information Disclosure | data.py import of DB_PATH | accept | DB_PATH is a local file path constant — no credential, no secret; acceptable for single-user local tool |
</threat_model>

<verification>
Full suite after Wave 0:
```
cd "Analista de Investimentos/12_PYTHON" && pytest tests/ -x -q
```
Expected: 115 prior tests + new stubs pass. No new failures introduced by package creation or app.py modification.

fpdf2 install check:
```
pip show fpdf2 | grep Version
```
Expected: `Version: 2.8.x` (or newer >=2.7.0)

Package import check:
```
python -c "from src.dashboard import data; from src.delivery.pdf_report import ReportGenerator; print('all imports OK')"
```
</verification>

<success_criteria>
- src/dashboard/__init__.py exists (Python package)
- data.py has 4 @st.cache_data functions, all SQL parameterized, try/finally conn.close(), thesis_json deserialized via json.loads()
- pdf_report.py has ReportGenerator(FPDF) subclass with generate_from_fixture() returning valid PDF bytes
- pyproject.toml lists fpdf2>=2.7.0; fpdf2 installed in venv
- _style.py has DARK_CSS constant with full CSS block from app.py
- app.py _PAGES has 9 total entries (5 original + 4 inteligencia_*); nav ratio is [1.4]+[1]*9
- All 4 test files exist with correct function names
- pytest on all 4 new test files exits 0
- Full suite (115+ tests) remains green
</success_criteria>

<output>
After completion, create `.planning/phases/05-delivery-layer/05-01-SUMMARY.md` following the summary template.
</output>
